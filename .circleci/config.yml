version: 2.1

jobs:
  build:
    working_directory: ~/circleci-python
    machine: true
    resource_class: kzexu9zaxcq5tjmkujg44l/test

    steps:
      - checkout
      - run: python3 main.py
  test:
    working_directory: ~/circleci-python
    machine: true
    resource_class: kzexu9zaxcq5tjmkujg44l/test

    steps:
      - checkout
      - run: python3 main-test.py
  
  deploy:
    working_directory: ~/circleci-python
    machine: true
    resource_class: kzexu9zaxcq5tjmkujg44l/test
    steps:
      - checkout
      - run: python3 main-deploy.py
  
workflows:
  build_and_test:
    jobs:
      - build:
          filters:
            branches:
              only: /.*(python|PYTHON)-\d+.*/
      - test:
          requires:
            - build
          filters:
            branches:
              only: /.*(python|PYTHON)-\d+.*/
      - hold:
          type: approval
          requires:
            - build
            - test
          filters:
            branches:
              only: /.*(python|PYTHON)-\d+.*/

      - deploy:
          requires:
            - hold
          filters:
            branches:
              only: /.*(python|PYTHON)-\d+.*/
      
    